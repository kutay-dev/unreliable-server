export enum Environment {
  DEV = 'dev',
  STG = 'stg',
  PROD = 'prod',
}

export enum Role {
  USER = 'USER',
  GOD = 'GOD',
}

export enum SendMessageType {
  POST = 'POST',
  PATCH = 'PATCH',
  DELETE = 'DELETE',
}

export enum FromCache {
  TRUE = 'true',
  PARTIAL = 'partial',
  FALSE = 'false',
}
