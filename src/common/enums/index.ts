export enum Environment {
  DEV = 'dev',
  STG = 'stg',
  PROD = 'prod',
}

export enum Role {
  USER = 'USER',
  GOD = 'GOD',
}

export enum FromCache {
  TRUE = 'true',
  PARTIAL = 'partial',
  FALSE = 'false',
}
